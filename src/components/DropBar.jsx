import React from 'react'
import './DropBar.css'

const colors = {
  1: 'red',
  2: 'yellow',
}

const DropBar = ( {currentPlayer} ) => {
  return (
    <div className="dropBar"> 
    </div>
  )
}

export default DropBar